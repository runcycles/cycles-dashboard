name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    uses: runcycles/.github/.github/workflows/ci-typescript.yml@55ce2a756806ee87d8c079fcd64662bd70fb5736 # main
    with:
      run-lint: true
