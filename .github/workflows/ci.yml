name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

jobs:
  ci:
    uses: runcycles/.github/.github/workflows/ci-typescript.yml@3d320e61800aaf763657a751600c788b38dc8e3b # main
    with:
      run-lint: true
